export interface WebUserLoginResponse {
  email: string;
  title: string;
  street_address: string;
  city: string;
  state: string;
  zip: string;
  country: string;
  phone: string;
}

export interface HolderUserLoginResponse {
  email: string;
}
